[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  path = "/api/*"
  function = "edge"

# Scheduled WIP lockdown — every Saturday at midnight ET (05:00 UTC)
[[scheduled_functions]]
  function = "lock-wip"
  cron = "0 5 * * 6"
